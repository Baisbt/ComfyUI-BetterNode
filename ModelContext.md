# ModelContext.md

项目上下文速查：已完成什么、当前在哪、为什么这么做、下一步做什么。

> 面向后续接手的人（或 AI 会话）。修改项目后请同步更新本文件。

---

## 1. 项目概述

**ComfyUI-BetterNode** —— 为 ComfyUI 默认节点提供统一的「输入参数」入口，
让原本只能在节点面板上手动填写的内部参数可被外部驱动，且**不修改任何现有节点代码**。

实现方式：纯前端扩展。`__init__.py` 只声明 `WEB_DIRECTORY`，不注册任何后端节点。

## 2. 环境基线（改动前请先确认）

| 组件 | 版本 / 位置 |
|---|---|
| ComfyUI | **0.30.2**（`comfyui_version.py`） |
| 前端包 | comfyui_frontend_package **1.47.12**（`python/Lib/site-packages/`） |
| KSampler 定义 | `nodes.py:1580`；KSamplerAdvanced 在 `nodes.py:1609` |

> **调研方法**：前端包自带 `.js.map`，`sourcesContent` 内含**未压缩源码**。
> 判断"某个能力能不能做、怎么做"时，先解析 source map 读源码，再下结论。
> 本项目的所有机制结论都附了「文件:行」出处，见 `TECHNICAL.md` §4 与 §11。

## 3. 当前进度

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | FR-1 / FR-2 / FR-4 / FR-5 —— 菜单、参数枚举、入参节点、一对多 | ✅ 完成 |
| M2 | FR-3 可连线参数 | ⛔ **已下线**（见 §5 决策 D3/D4） |
| M3 | FR-6 状态反馈 / FR-7 校验与中英双语 | ✅ 完成 |
| — | FR-8 入参节点随机化 | ✅ **无需开发**（原生已满足，见决策 D2） |
| — | FR-9 批量外置 | ✅ 完成 |

**回归测试 36 项全部通过**：`node tests/run.mjs`

### 未验证项（功能已实现，但真机未跑过）

1. **「合并被拒」提示** —— 核心节点中难以构造复现场景；逻辑有单元测试覆盖
2. **批量外置的撤销行为** —— "一次 Ctrl+Z 整体回退"目前只有逻辑保证

## 4. 架构速查

```
右键节点 → getExtraMenuOptions（链式包装，不覆盖其他插件）
  └─ buildMenu
       ├─ guards.assessNode / assessParam   → 能不能做、为什么不能
       ├─ enumerate.enumerateParams         → 列出内部编辑参数（input.widget 存在者）
       ├─ createInputNode.getExternalizeDetail → 复用明细（状态 / 数量 / 同义组）
       └─ 点击 → createInputNode.createOrReuseInputNode
                    └─ 复用内置虚拟节点 PrimitiveNode
```

关键点：

- **分类判据**：`input.widget` 存在 = 内部编辑参数（本版本 widget 与 socket 共存，槽已由核心创建）
- **入参节点**：直接复用 ComfyUI 内置的 `PrimitiveNode`，**不自研同步逻辑**
  （取值拷贝、下拉选项同步、一对多、值控制控件全部原生支持）
- **复用索引**：写入 `node.properties.betterNode`（`properties` 会被序列化，可跨会话）
- **复用键**：参数名经同义组归并后的键；**不落盘**，一律由 `marker.param` 现算
- **相对导入**：`WEB_DIRECTORY` 内容被服务于 `/extensions/<插件目录名>/`，因此
  `web/x.js` 用 `../../scripts/app.js`，`web/actions/x.js` 用 `../../../scripts/app.js`

## 5. 关键决策记录

> 这一节最有价值 —— 请勿在未读完之前推翻既有方案。

| # | 决策 | 原因 / 证据 |
|---|---|---|
| **D1** | 入参节点**复用内置 PrimitiveNode**，不自研 | 该虚拟节点已具备全部所需能力：首次连接按目标规格建控件、拷贝当前值、下拉选项同步、一节点连多目标（含范围合并） |
| **D2** | FR-8 随机化**不做实现** | 值控制控件由**控件构造器**自动添加（条件：声明了 `control_after_generate`，或参数名为 `seed`/`noise_seed`）；PrimitiveNode 里那段只是兜底。曾因只看到兜底分支而误判为"不生效" |
| **D3** | 菜单内**起线拖拽**方案否决 | 原生落点依赖 `pointerdown` 写入的 `eDown`；菜单点击无此动作 → `onDragEnd` 永不触发；合成指针序列也无法绕过（`move()` 在 `!e.buttons` 时立即复位）。**详见 `TECHNICAL.md` §5.4.1，勿重试** |
| **D4** | FR-3 整体下线，可连线参数不进菜单 | D3 的替代方案（来源选择菜单）虽可工作，但交互比手动连线更绕。用户改用 ComfyUI 原生拖线 |
| **D5** | 不兼容时新建**专用节点**（`owner` 标记） | 早期只写"不兼容时新建节点"，未考虑重复点击会让节点数无限膨胀。引入 `owner` 后查找优先命中专用节点，行为幂等 |
| **D6** | 复用键不落盘，现算 | 新增同义组后，**旧工作流里已存在的节点也能被正确归并，无需数据迁移** |
| **D7** | 同义组用**人工白名单** | 禁止按名称相似度自动归并。加组必须核实「是否传给同一参数位」+「输入规格是否一致」。`seed`/`noise_seed` 已核实两者传给 `common_ksampler` 的同一参数位 |
| **D8** | 复用明细只做**条目文案后缀** | 转换时会丢弃 `title` / `className`，且 `MenuOption` 无 tooltip 字段 —— 菜单内**做不了悬浮提示**，也无法嵌套子菜单。故节点标题/类型/取值有意未纳入 |
| **D9** | 不可用项一律**置灰并说明**，不静默隐藏 | 用户需要知道"为什么少了参数" |
| **D10** | `getExtraMenuOptions` 必须**链式包装** | 直接赋值会覆盖其他插件的同名钩子（最常见的插件间冲突） |

## 6. 待办

### 可做

1. 补 `LICENSE`（当前未指定）
2. 英文版 README（当前仅中文）
3. 一个参数同时存在「共享节点 + 多个专用节点」时，菜单是否展示更细的复用明细

### 明确不做

- **菜单内连线能力**：不在计划内。若将来重启，须先解决 D3 的指针机制问题，
  并重新评估交互形式（需自行实现输出槽命中判定，核心逻辑内联在 `processMouseDown` 中未暴露）

### 暂不支持（有意置灰）

子图节点参数、自定义 widget 类型（curve / imagecrop 等）、`socketless` 参数。

## 7. 开发思路与约定

- **投产前先验证**：本项目两次因"静态推断"得出错误结论（D2、D3），
  凡涉及前端内部行为的判断，**能用实测就用实测**
- **找齐所有生产者**：判断"某能力是否存在"时必须搜遍全部产生它的代码路径。
  D2 的误判就是因为只看了一处
- **改目录结构后先跑测试**：`tests/run.mjs` 会还原真实 URL 层级，
  相对导入写错会立刻暴露
- **只做加法**：不改 ComfyUI 核心、不改任何现有节点、不改变原生节点序列化格式
- **异常隔离**：插件内部异常一律 catch，不得影响核心与其他插件的菜单
- **提交粒度**：按逻辑单元提交，信息格式 `类型: 简述`

## 8. 文档索引

| 文档 | 用途 |
|---|---|
| `README.md` | 面向使用者：安装、使用、支持范围 |
| `DESIGN.md` | 面向产品：需求清单 FR-1~FR-9、交互流程、验收标准 AC-1~AC-22 |
| `TECHNICAL.md` | 面向实现：架构、源码依据、模块设计、兼容降级、测试矩阵、决策记录 |
| `tests/run.mjs` | 逻辑回归测试（无需启动 ComfyUI） |
