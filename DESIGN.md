# DESIGN.md — BetterNode 产品设计文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.3 |
| 状态 | 待审阅 |
| 关联文档 | `TECHNICAL.md`（技术方案）、`AGENTS.md`（开发约束） |
| 目标环境 | ComfyUI 0.30.2 + 前端包 1.47.12 |
| 变更记录 | v1.1 新增 FR-7、FR-8，FR-3 改为双入口，新增 §6.1 与 AC-11~AC-17<br>v1.2 FR-3 拖拽方案实测否决<br>v1.3 **FR-3 整体下线**：「输入参数」只列内部编辑参数；FR-2 改为单组展示并去掉分组后缀；新增空菜单边界；AC-11 改为"可连线参数不出现在菜单中" |

---

## 1. 产品定位

### 1.1 要解决的问题

ComfyUI 默认节点的参数在用户心智中分为两类：

- **可连线参数**：节点上以输入槽形式存在（如 KSampler 的 `model`、`positive`），天然可被上游节点驱动。
- **内部编辑参数**：只能在节点面板上手动输入（如 KSampler 的 `seed`、`steps`、`cfg`、`sampler_name`），难以被外部统一驱动。

当用户需要「一个随机种子同时驱动 3 个采样器」「步数与 CFG 由上游计算节点统一下发」「采样器/调度器集中切换」时，逐个节点手动改值既繁琐又容易不一致。

**BetterNode 的目标**：为任意默认节点提供一个统一的「输入参数」入口，让全部参数都能被外部驱动，且**不修改任何现有节点代码**。

### 1.2 与 ComfyUI 原生能力的边界（重要）

经源码级核实，本版本（前端 1.47.12）中 widget 与 socket **是共存的**：

- 每个非 `forceInput`、非 `socketless` 的 widget 参数，节点上**已经有对应的输入槽**。
- 用户已可通过「把连线拖到 widget 上」或「双击 widget 输入槽自动挂载 Primitive 节点」实现外部驱动。
- 旧的 `convertWidgetToInput` 已被官方标记为废弃并成为空操作。

因此必须明确：**本插件不是"解锁原生做不到的能力"，而是把既有能力变成可发现、可一键、可总览的操作。**

| 维度 | 原生 | BetterNode 增量 |
|---|---|---|
| 发现性 | 需要用户知道"widget 可被连线" | 右键菜单集中列出全部参数 |
| 效率 | 需精确定位并拖拽到目标 widget | 点一下即完成起线或建节点 |
| 总览 | 无法一眼看全节点参数与槽位状态 | 二级菜单给出参数全貌与状态标识 |
| 一对多 | Primitive 节点可多连，但需手动逐个连 | 同参数自动复用同一入参节点 |

> 2026-09-17 已就该定位差异与用户确认，用户认可上述定位。

### 1.3 非目标（Non-goals）

- ❌ 不修改 ComfyUI 核心或任何现有节点/自定义节点的源码。
- ❌ 不改动后端执行逻辑，不新增会参与执行的节点类型。
- ❌ 不改变原生节点的序列化格式（工作流文件保持向后兼容）。
- ❌ 不追求兼容旧版本前端（见 §9）。

---

## 2. 目标用户与典型场景

**目标用户**：需要批量、联动控制生成参数的 ComfyUI 使用者。

| 场景 | 描述 | 涉及需求 |
|---|---|---|
| A 一种子多采样 | 一个随机种子同时驱动 3 个 KSampler 的 `seed`，保证多阶段采样噪声一致 | FR-4、FR-5 |
| B 参数下发 | `steps` / `cfg` 由上游计算节点统一下发，改动一处全流程生效 | FR-3、FR-5 |
| C 集中切换 | `sampler_name` / `scheduler` 下拉参数集中切换，一次改动影响全部采样器 | FR-4、FR-6 |
| D 快速外置 | 用户想给 `model` 这类纯连线条目起线，但不想精确瞄准小圆点 | FR-3 |

---

## 3. 术语定义

| 术语 | 定义 |
|---|---|
| **参数槽** | 节点上的一个输入槽（`node.inputs[i]`） |
| **可连线参数** | 参数槽中**未绑定 widget** 的项，如 `model`、`positive`、`latent_image` |
| **内部编辑参数** | 参数槽中**已绑定 widget** 的项（`input.widget` 存在），如 `seed`、`steps`、`cfg`、`sampler_name` |
| **入参节点** | 为内部编辑参数生成的、可向一个或多个节点赋值的独立节点（本方案复用 ComfyUI 内置 `PrimitiveNode`） |
| **起线** | 使画布进入"从某个参数槽拖出连线"的交互状态 |

---

## 4. 功能需求

### FR-1 菜单入口

- 在任意节点的右键菜单中新增一项，文案为**可配置的多语言标题**（中文环境显示「输入参数」）。
- 该项为**二级菜单**，点击展开参数列表。
- 节点若没有任何可外置参数，该项**不注入**（不出现空菜单）。

### FR-2 参数枚举与展示

- **范围**：只列出**内部编辑参数**（即 `input.widget` 存在者）。可连线参数不再出现（见 FR-3）。
- **排序**：与节点定义（`nodeDef.input_order` / `inputs` 顺序）保持一致，不重排。
- **排除项**：不列出控件类联动 widget（如 `seed` 附带的 `control_after_generate`），避免噪声。
- **空菜单处理**：节点若没有任何内部编辑参数，「输入参数」整项**不注入**。
- **状态标识**：因二级菜单不支持分隔线（`TECHNICAL.md` §4 机制 6），状态通过**文案后缀**表达：

| 示例文案 | 含义 |
|---|---|
| `步数` | 未外置 |
| `种子 · 已外置` | 已存在入参节点，且已连到本节点 |
| `种子 · 可复用` | 已存在入参节点，但尚未连到本节点 |

### FR-3 可连线参数交互 —— ⛔ 已下线

**状态：已下线（2026-09-17，经评审决定）**

- **原设计**：在「输入参数」菜单中一并列出可连线参数（如 `model`、`positive`），点击后建立连线。
- **实现过程**：先后尝试两种交互 —— ①「起线拖拽态」（点击后连线跟随鼠标、点目标槽落点）；
  ②「来源选择菜单」（点击后弹出原生来源列表，选中即建链）。
- **下线原因**：
  - 方案 ① 经实测**只能出现连线动画、无法建立连接**（根因见 `TECHNICAL.md` §5.4.1）；
  - 方案 ② 虽可工作，但**交互体验不符合预期，比手动连线更绕**。
- **决定**：彻底取消该功能。可连线参数**不再出现在**「输入参数」菜单中，相关代码已删除。
- **用户仍可正常连接这些参数**：ComfyUI 原生支持把连线直接拖到参数槽上，无需插件提供入口。
- 保留本节仅为留存决策与原因，避免将来重复尝试。

### FR-4 内部编辑参数交互

- 点击菜单中的参数 → 在画布上生成一个**入参节点**，并将其输出连接到该参数槽。
- 入参节点的值 = **所选参数当前的取值**（不改变原值，仅做拷贝）。
- 若该参数为**下拉框**（combo，如 `sampler_name` / `scheduler`），入参节点的下拉选项须与该节点保持一致。
- 入参节点标题为参数可辨识名称（如 `入参·seed`）。
- 新节点须自动摆放在不与其他节点重叠的位置。

### FR-5 一对多赋值

- **同一个入参节点须能同时给多个节点赋值**，包括同类型节点的同名参数。
- 重复点击同名参数时，默认**复用**已存在的入参节点，而不是新建。
- 复用后，入参节点的数值变更须同步影响全部已连接目标。
- 数值型参数在合并不同目标时，取值范围按更严格的交集收敛，且不得使已连接目标的值越界。
- **兼容性失败的处理**：若某个目标与已有入参节点的类型或取值范围不兼容（无法合并），
  则改为**为该目标单独创建一个"专用入参节点"**，而不是拒绝操作，也不影响其他已连接目标。
- **幂等要求**：上述专用节点建立后，重复点击同一参数必须**直接命中原有节点**，
  不得产生新的节点，也不得重复告警。

### FR-6 状态反馈

- 已外置的参数在菜单中带状态标识（见 FR-2）。
- 入参节点被删除、或连线被断开后，相关参数的状态标识须回到未外置。

### FR-7 校验与错误引导（含中英文）

分三层呈现，不得静默处理。

| 层级 | 触发场景 | 呈现方式 |
|---|---|---|
| **阻断级** | 参数不可外置（socketless / 无输入槽）、节点类型不支持、前端版本不支持、当前已有连线进行中、槽被锁定 | 菜单项**置灰** + 悬浮说明原因；必要时弹窗 |
| **提示级** | 值因范围合并被钳制、跨节点类型复用导致下拉选项收窄、合并被拒绝（类型或范围不兼容） | 一次性提示，**说明原因**而非仅报告结果 |
| **说明级** | 常见报错的中英对照引导 | 插件自带语言字典，按前端语言设置自动切换 |

要求：

- 语言字典须**同时提供中文与英文**两套文案，覆盖以上全部三层。
- 语言选择依据前端的语言设置自动判定，用户无需手动配置。
- 文案须指向可执行的下一步（例如"该参数无法外置，因为节点声明了 socketless"），不得只输出错误码。

### FR-8 入参节点随机化能力

- 凡**数字型**（`INT` / `FLOAT`）与**下拉型**（combo）参数的入参节点，须保证具备**随机化**能力。
- 随机化的生效时机（入队前 / 入队后）遵循前端已有的全局设置，插件不另行定义。

> **实测结论（2026-09-17，前端 1.47.12）：该能力由 ComfyUI 原生完全满足，插件无需实现任何代码。**
> 控件构造器在「参数声明了 `control_after_generate`」或「参数名为 `seed` / `noise_seed`」时
> 自动添加值控制控件（`fixed` / `increment` / `decrement` / `randomize`，默认 `randomize`）；
> PrimitiveNode 仅在目标参数未声明时兜底补一个，用于避免重复添加。
> 两条路径合计覆盖全部数字型与下拉型参数，已在真实环境验证（`入参·noise_seed`）。
> 本需求降级为**验收项**，不再是实现项。

---

## 5. 交互流程

### 5.1 主流程（状态机）

```
空闲
 └─ 右键节点 ──▶ 菜单展开（仅单选节点时出现「输入参数」）
      ├─ 点「输入参数」──▶ 子菜单展开（只列内部编辑参数）
      │    └─ 点某个参数 ──▶ 建/复用入参节点 + 自动连线 ──▶ 空闲
      └─ 点其他项 / 空白 ──▶ 空闲
```

### 5.2 异常分支

| 情况 | 预期行为 |
|---|---|
| 参数为 `socketless` 控件或运行时无输入槽 | 条目**置灰**并说明"该参数不支持外置"（阻断级） |
| 节点为子图节点 / 特殊虚拟节点 | 条目**置灰**并说明不支持（阻断级） |
| 参数槽被锁定 | 条目**置灰**并说明（阻断级） |
| 节点没有任何内部编辑参数 | 「输入参数」项**不注入** |
| 节点处于 Bypass / Mute 状态 | 仍允许操作，但在条目上标注节点当前状态，避免误以为参数生效 |
| 合并到已有入参节点被拒绝（类型或范围不兼容） | 为该目标**新建专用入参节点**，并给出提示级说明；不影响其他已连接目标 |
| 合并成功但原值被钳制 | 提示级说明"取值范围已收敛，值已调整"，并给出调整前后取值 |

---

## 6. 边界与约束

1. **零侵入**：不修改 ComfyUI 核心、`nodes.py`、`comfy_extras` 或任何第三方节点的源码；插件全部逻辑位于自身目录内，通过前端扩展机制在运行时挂载。
2. **仅作用于画布已有节点**：不改变节点库中的节点定义，不影响新建节点的行为。
3. **序列化兼容**：不改变原生节点的序列化字段；只新增入参节点自身的序列化数据。
4. **可撤销**：入参与连线操作须纳入画布的撤销/重做历史。
5. **不污染菜单**：除「输入参数」一项外，不向节点菜单注入其他条目。
6. **不改动既有参数值**：创建入参节点时拷贝目标当前值（值相同），不构成对原节点的修改。

### 6.1 v1 支持范围

| 对象 | v1 策略 | 说明 |
|---|---|---|
| 标准 INPUT_TYPES 节点（含绝大多数第三方插件） | ✅ 支持 | 走统一注册管线，自动适配 |
| 高级（advanced）/隐藏（hidden）的内部参数 | ✅ 支持 | 正常列出 |
| 已被连线的内部参数槽 | ✅ 支持 | 正常列出；已有外置节点时标注状态 |
| Bypass / Mute 节点 | ⚠️ 支持但标注 | 操作有效，但执行语义已变 |
| `socketless` 参数、运行时无输入槽的 widget | ⛔ 置灰并说明 | 原生不为其建槽，无法外置 |
| 自定义 widget 类型（curve / imagecrop / colors 等） | ⛔ 仅置灰并说明 | 入参节点会退化为通用控件，外观不一致 |
| 子图节点（SubgraphNode） | ⛔ 置灰并说明 | 其参数为"提升"的伪 widget，规格解析路径不同 |
| 虚拟 / 非执行节点（Note、MarkdownNote、Reroute、PrimitiveNode 自身） | ⛔ 不注入菜单 | 无参数语义 |
| 分组（LGraphGroup） | ⛔ 不受影响 | 分组菜单与节点菜单是两条通道 |

**原则**：不可用项一律**置灰 + 说明原因**，不静默隐藏 —— 用户需要知道"为什么少了参数"。

---

## 7. 验收标准

| ID | 验收项 | 判定方式 |
|---|---|---|
| AC-1 | 右键 KSampler 出现「输入参数」，展开为二级菜单且参数分两组 | 目视 |
| AC-2 | 菜单中的参数条目与节点实际输入参数一一对应，无遗漏、无多余联动控件 | 与 `INPUT_TYPES` 对照 |
| AC-3 | 点击 `model` → 画布立即进入连线拖拽态，可连到任意 `MODEL` 输出 | 目视 + 实际连线成功 |
| AC-4 | 点击 `seed` → 生成入参节点，其值等于该 KSampler 当前 seed | 对照取值 |
| AC-5 | 点击 `sampler_name` → 入参节点为下拉框，选项与 KSampler 完全一致 | 逐项比对 |
| AC-6 | 一个入参节点输出连到 3 个 KSampler 的 `seed`，改值后三者同步 | 运行验证 |
| AC-7 | 再次点击另一个 KSampler 的 `seed` → 复用已有入参节点而非新建 | 目视节点数量 |
| AC-8 | 操作可正确撤销/重做 | 快捷键验证 |
| AC-9 | 保存工作流 → 重载 → 入参节点、连线与取值均正确恢复 | 重载验证 |
| AC-10 | `git diff` / 文件比对确认未改动任何原生文件 | 版本控制核对 |
| AC-11 | 可连线参数（`model` / `positive` 等）**不出现在**「输入参数」菜单中 | 目视 |
| AC-12 | 对 `socketless` 参数、子图节点、自定义 widget，条目置灰且能读到原因说明 | 目视 |
| AC-13 | 合并导致取值被钳制时，出现说明原因的提示（非静默） | 目视 |
| AC-14 | 插件全部提示文案在中文与英文环境下均正确显示，无缺漏、无硬编码 | 切换前端语言验证 |
| AC-15 | 数字型与下拉型入参节点均具备随机化能力（由 ComfyUI 原生提供，插件不额外实现） | 实际操作 + 队列验证 |
| AC-16 | 多选节点右键时，「输入参数」不出现 | 目视 |
| AC-17 | 节点处于 Bypass 状态时，菜单条目带有状态提示 | 目视 |
| AC-18 | 与已有入参节点不兼容的参数：首次点击新建专用节点，**再次点击直接命中且不新增节点、不重复告警** | 目视 + 节点计数 |

---

## 8. 兼容性要求

- **前端基线**：comfyui_frontend_package **1.47.12**（本机实测版本）。
- 仅保证在上述版本可用；其他版本不承诺（若版本不符，插件应给出明确提示而非静默失效）。
- 启动时做能力探测，关键 API 缺失时禁用功能并提示原因（详见 `TECHNICAL.md` §7）。

---

## 9. 风险与依赖

| 风险 | 影响 | 应对 |
|---|---|---|
| 依赖前端内部 API（`showConnectionMenu`、`PrimitiveNode` 行为），非官方稳定契约 | 版本升级后可能失效 | 启动期能力探测 + 集中式兼容层 + 明确降级提示 |
| ~~从菜单点击启动连线，可能缺少原生指针生命周期钩子~~ | ~~FR-3 可能不可用~~ | **已实测否决，FR-3 整体下线**，可连线参数不再进入菜单（见 FR-3 决策记录） |
| 二级菜单不支持分隔线与分组标题 | 分组只能靠文案前缀 | 已纳入 FR-2 设计 |
| 菜单项被归入「Extensions」分组，无法插入核心项之间 | 位置不如预期显眼 | 接受；文案设计保证可辨识 |
| `socketless` 控件无输入槽 | 该类参数无法外置 | 明确列出但置灰并说明 |
| 取值范围不同的同类参数被合并 | 目标取值可能被裁剪 | 采用更严格交集，并**提示用户**（FR-7 提示级） |
| 「合并被拒」在核心节点中难以构造复现场景 | 该提示路径未经真实验证 | 逻辑已有单元测试覆盖；必要时后补调试入口 |
| 前端未对外暴露 i18n 接口 | 无法复用官方翻译，双语字典需自行维护 | 插件自带字典 + 按语言设置切换；字典集中管理便于扩展语种 |
| 入参节点随机化能力依赖原生行为 | 曾误判其触发条件 | 已实测确认原生完整覆盖，插件不实现（见 FR-8） |
| 第三方插件的自定义 widget 形态各异 | 入参节点外观可能与原节点不一致 | v1 对这类置灰并说明（见 §6.1） |

---

## 10. 里程碑

- **M1** 核心链路：FR-1 / FR-2 / FR-4 / FR-5 + 基础置灰与说明 ✅ 已完成
- **M2** FR-3 可连线参数 ⛔ **已下线**（拖拽方案实测不可用，来源菜单方案体验不符预期）
- **M3** FR-6 状态反馈 + FR-7 校验与中英双语引导 ✅ 已完成（FR-8 由原生满足，仅需验收）
- 后续可评估：按语义合并同义参数（`seed` 与 `noise_seed`）、入参节点样式自定义、批量外置整节点参数、自定义 widget 类型与子图节点的完整支持。
- **不计划重启**：菜单内连线能力。用户可继续使用 ComfyUI 原生的拖线方式。详见 `TECHNICAL.md` §5.4。
